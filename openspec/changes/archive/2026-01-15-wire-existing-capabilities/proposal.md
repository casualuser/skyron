# Change: Wire Existing Capabilities

## Why
The project has an existing implementation but lacks formal specifications. This change baselines the current capabilities into OpenSpec to enable spec-driven development moving forward.

## What Changes
- [ADDED] `blockchain-core` capability
- [ADDED] `transaction-management` capability
- [ADDED] `mining-operations` capability
- [ADDED] `network-consensus` capability

## Impact
- Affected specs: N/A (Initial baseline)
- Affected code: Includes all existing logic in `app.py` and `skyron/` package.
